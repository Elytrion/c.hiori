#pragma once

namespace chiori
{
	namespace commons
	{
		constexpr float EPSILON = 0.0001f;		// Epislon value for comparing floats
		constexpr float LEPSILON = 0.01f;		// Low precision epsilon value
		constexpr float HEPISLON = 0.000001f;	// High precision epsilon value
		constexpr float PI = 3.14159265f;
		constexpr float DEG2RAD = PI / 180.0f;
		constexpr float RAD2DEG = 180.0f / PI; 
	}
}